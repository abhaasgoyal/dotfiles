/* Author: Abhaas Goyal */
#include <bits/stdc++.h>
using namespace std;
#define F first
#define S second
#define PB push_back
#define MP make_pair
#define repe(iter, a, b) for (int iter = a; iter <= b; iter++)
#define rep(iter, a, b) for (int iter = a; iter < b; iter++)
typedef long long ll;
typedef vector<int> vi;
typedef vector<ll> vll;
typedef pair<int, int> pi;
typedef pair<ll, ll> pll;

void solution() {
  /*  -- YOUR CODE HERE -- */

  /* -- YOUR CODE HERE -- */
}
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cout.precision(17);
  int t = 1;
  while (t--) {
    solution();
  }
  return 0;
}
